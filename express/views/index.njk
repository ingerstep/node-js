<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title>({{ count }}) Counter</title>
    {{
      uiKitCss | safe
    }}

    <link rel="stylesheet" type="image/x-icon" href="/favicon.ico">
  </head>
  <body>
    <section class="uk-container">
      <h1>The count is: {{ count }}</h1>

      {% if count> 20 %}
      <h2>WOAH!</h2>
      {% endif %}

      <p>
        <button id="btn">Increment it</button>
      </p>

      <p>{{ uiKitCss }}</p>

      {% if count > 0 %}
      <ul>
        {% for cnt in counts %}
        <li>{{ cnt }} bottles of beer on the wall</li>
        {% endfor %}
      </ul>
      {% endif %}
    </section>

    <script>
      document.getElementById("btn").addEventListener("click", () => {
        fetch("/inc", { method: "post" }).then(() => {
          location.reload();
        });
      });
    </script>
  </body>
</html>
